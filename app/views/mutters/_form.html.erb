<html>
  <body>
    <main>
<%= form_with(model: @mutter, local: true ,url: choose_new_or_edit ) do |form| %> 
<% if @mutter.errors.any? %>
      <p><%= @mutter.errors.count %>件のエラーがあります。<br>
      １文字以上１４０文字以内でつぶやいて下さい。</p>
      <ul>
      <% @mutter.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  </div>
  <div class="mutter_title">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>
  <%= form.submit %>
<% end %>
<%= link_to "つぶやき一覧画面にもどる", mutters_path %>
    </main>
  </body>
</html>


